parameters:
parameters:
  - name: LoopList
    type: object
    default: $[ 5 ]
 
steps:
  - ${{ each value in parameters.LoopList }}:
    - script: echo ${{ value }}
      displayName: 'Test looping'
